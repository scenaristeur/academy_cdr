import { createStore } from 'vuex'
import config from './modules/config'


export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    config
  }
})
